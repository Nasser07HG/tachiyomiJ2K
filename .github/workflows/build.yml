name: Build APK and Release

on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v3

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Disable Google Services Plugin
        run: |
          find . -name "build.gradle*" -type f -exec sed -i '/google-services/d' {} +
          rm -rf app/google-services.json app/src/*/google-services.json

      - name: Build APK (Release)
        run: ./gradlew assembleStandardRelease -x lint -x test

      - name: Compress APK Package
        run: |
          mkdir -p final_output
          zip -j final_output/app-release.zip app/build/outputs/apk/release/*.apk

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: TachiyomiJ2K-Release-Build
          path: final_output/app-release.zip
          
